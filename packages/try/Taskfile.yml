version: "3"

includes:
  try-lightbug:
    aliases: [ 'tl', ]
    taskfile: try-lightbug/
    dir: try-lightbug/
    optional: true

################################################################################################

tasks:
  default:
    cmds:
      - echo 'hello'
